<%= render partial: 'shared/navbar' %>
<h1>Agenda Edit</h1>
<%= form_for @agenda, url: agenda_path, method: 'patch' do |f| %>
<%= hidden_field_tag "agenda[mom_id]", "#{@agenda.mom.id}" %>
<%= render partial: 'agendas/forms', locals: {f: f} %>  
<% end %>